type: object
properties:
  name:
    type: string
    description: Task name
  status:
    type: string
    description: Task status for team
    enum: [pending, solved]
required:
  - name
  - status
